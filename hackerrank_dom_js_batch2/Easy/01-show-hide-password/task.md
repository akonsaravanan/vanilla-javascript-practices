# Easy - Show Hide Password

Requirements:
1. Password input starts as `type="password"`.
2. Clicking the toggle button should switch the input type between `password` and `text`.
3. Button label should switch between `Show` and `Hide`.
