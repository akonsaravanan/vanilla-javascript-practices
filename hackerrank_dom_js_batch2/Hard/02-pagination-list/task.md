# Hard - Pagination List

Requirements:
1. Render the items from `window.ITEMS` into `#page-list`.
2. Show 3 items per page.
3. `Prev` and `Next` buttons should navigate pages.
4. Update `#page-info` as `Page X of Y`.
5. Prevent page number from going out of bounds.
