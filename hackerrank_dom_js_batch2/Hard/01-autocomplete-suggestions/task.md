# Hard - Autocomplete Suggestions

Requirements:
1. Use the provided `window.SUGGESTIONS` array.
2. Typing in the input should show matching suggestions in the list.
3. Matching is case-insensitive and based on `startsWith`.
4. Clicking a suggestion should put its value into the input and clear the list.
