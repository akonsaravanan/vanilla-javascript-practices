# Medium - Modal Overlay Escape

## Goal
Write JavaScript so that:
1. Clicking `Open Modal` opens the modal.
2. Clicking `Close` closes the modal.
3. Clicking the dark overlay closes the modal.
4. Pressing the `Escape` key closes the modal.
