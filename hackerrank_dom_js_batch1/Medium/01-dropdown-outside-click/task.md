# Medium - Dropdown Outside Click

## Goal
Write JavaScript so that:
1. Clicking the `Menu` button toggles the dropdown open/closed.
2. Clicking anywhere **outside** the dropdown closes it.
3. Clicking inside the dropdown should not close it immediately.

## Hint
You may need event bubbling knowledge and `contains()`.
