# Easy - Tabs Switcher

## Goal
Write JavaScript so that:
1. The **first tab** is active by default.
2. Only **one tab content** is visible at a time.
3. Clicking a tab shows its matching content and hides others.
